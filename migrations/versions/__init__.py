"""Placeholder — Alembic will generate migration files here.

Run to generate first migration:
    alembic -c migrations/alembic.ini revision --autogenerate -m "initial_schema"

Run to apply:
    alembic -c migrations/alembic.ini upgrade head
"""
